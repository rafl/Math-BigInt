This lists only the changes for the current release (v1.57) over the last
version, since HISTORY is now quite big:

Calc  :
MBI   : fix in objectify() to make "perl -Mbigrat -le 'print 1+2/3'" work
MBF   : upgrade used badd() instead of bmul() inside bmul()
tests :

Please have Math::BigInt->new('inf')->bmul('inf') big amounts of fun.

Tels <http://bloodgate.com/perl>


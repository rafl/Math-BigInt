This lists only the changes for the current release (v1.77) over the last
version, since HISTORY is now quite big:

Calc : remove shortcut in div(), since it doesn't work
MBI  : bump version
MBF  : fix new() to work correctly with Math::BigInt::Pari
tests: tests for div ((2**100)-1)/2**100

##############################################################################

Please have Math::BigInt->bzero()->bpow('0.1')->bpow(-42) big amounts of fun.

Tels <http://bloodgate.com/perl>


